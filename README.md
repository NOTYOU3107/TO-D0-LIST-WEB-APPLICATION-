# TO-D0-LIST-WEB-APPLICATION-
A lightweight, self-contained web-based to-do list app built entirely with pure Java. It uses the built-in com.sun.net.httpserver.HttpServer to handle requests, requiring no external frameworks. Users can add, complete, and delete tasks through a simple, clean interface. Fast to run, easy to use, and fully standalone.
